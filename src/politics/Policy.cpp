#include "hiersim/Policy.h"
// Policy is a simple data struct containing policy definitions
// All logic is handled by the Politics module and Coalition system
// No additional implementation needed beyond the header definition
namespace hiersim {
// Policy implementations are inline in the header file
}
